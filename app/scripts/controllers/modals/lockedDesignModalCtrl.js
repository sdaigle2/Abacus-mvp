'use strict';

/**
 * @ngdoc function
 * @name abacuApp.controller:LockedDesignModal
 * @description
 * # LockedDesignModal
 * Controller of the abacuApp
 */
angular.module('abacuApp')
  .controller('LockedDesignModalCtrl', ['$scope',
    function ($scope) {
      // Doesn't do anything now...may want to add stuff here later
    }]);
